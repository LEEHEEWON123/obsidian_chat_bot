# Agent notes

- This repo is **indexing + Qdrant + MCP** only. Chat UI is Hermes Workspace (external).
- No Next.js app. Prefer `npm run mcp`, `npm run index`, Hermes scripts.
- Path alias `@/*` → repo root (`lib/`, `scripts/`).
