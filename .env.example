# Obsidian vault 또는 Git 레포 경로 (절대 경로)
VAULT_PATH=/path/to/your/vault

# Cursor SDK API 키 — https://cursor.com/settings
CURSOR_API_KEY=

# 임베딩: local | openai (MVP 기본: local)
EMBEDDING_MODEL=local

# 인덱싱 glob (MVP: markdown만)
INDEX_INCLUDE=**/*.md
